import { Button, Container, Title } from '@mantine/core';

export default function Home() {
  return (
    <Container>
      <Title>Welcome to Mantine with React Router</Title>
      <Button>Test Button</Button>
    </Container>
  );
}